# practice.py

def is_even(a):
  #if a % 2 == 0:
  # return True
  #else:
  #  return False
return a % 2 == 0
def opposite(a, b):
  # if (a >= 0 and b>=0) or (a < 0 and b < 0):
  # return False
  # else:
   return True
